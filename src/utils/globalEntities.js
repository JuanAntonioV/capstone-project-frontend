export const storeStatus = {
    idle: 'idle',
    loading: 'loading',
    succeeded: 'succeeded',
    failed: 'failed',
};

const MENUNGGU_PEMBAYARAN = 1;
const PROSES = 2;
const SELESAI = 3;
const GAGAL = 4;
const DIBATALKAN = 5;

export const SALES_STATUS = {
    MENUNGGU_PEMBAYARAN,
    PROSES,
    SELESAI,
    GAGAL,
    DIBATALKAN,
};

const AKTIF = true;
const NON_AKTIF = false;

export const STATUS = {
    AKTIF,
    NON_AKTIF,
};
